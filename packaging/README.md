# Packaging scaffolding

This directory contains starter packaging metadata for downstream maintainers:

- `debian/`: Debian/Ubuntu-style package scaffolding.
- `rpm/scrollwm.spec`: RPM package scaffold for Fedora/openSUSE-like systems.

These files are intentionally minimal and should be adapted for target distro policy.
